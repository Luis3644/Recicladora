import 'dart:typed_data';

Future<void> downloadFileImpl(
  Uint8List bytes,
  String fileName,
  String mimeType,
) async {
  throw UnsupportedError('Descarga web no disponible en esta plataforma.');
}
